import styled from "styled-components"

export const StyledMain = styled.main`
  padding: var(--head-spacer) 0 var(--footer-spacer) 0;
  width: 80%;
  margin: 0 auto;
  height: calc(100vh - (var(--head-spacer) + var(--footer-spacer)));
`
